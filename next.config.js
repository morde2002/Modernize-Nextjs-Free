/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',  // Static export for your Next.js project
  trailingSlash: true,  // Ensure paths end with a slash
};

module.exports = nextConfig;
